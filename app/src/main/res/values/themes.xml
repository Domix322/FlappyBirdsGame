<resources>
    <!-- Base application theme. -->
    <style name="Theme.FlappyBirdGame" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Полноэкранная тема для игрового экрана (визуальный агент). -->
    <style name="Theme.FlappyBirdGame.Game" parent="Theme.FlappyBirdGame">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- Стартовый фон: небо игры + название по центру. -->
        <item name="android:windowBackground">@drawable/splash_window</item>
    </style>
</resources>